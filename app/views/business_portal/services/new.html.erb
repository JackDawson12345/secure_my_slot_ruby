<main class="p-4 sm:p-6 lg:p-8">
  <div class="mx-auto">
    <div class="mb-6">
      <%= link_to business_services_path,
                  class: "inline-flex items-center gap-2 text-sm font-semibold text-slate-500 transition hover:text-indigo-600" do %>
        <i data-lucide="arrow-left" class="h-4 w-4"></i>
        Back to services
      <% end %>
    </div>

    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
      <header class="border-b border-slate-200 p-6">
        <h1 class="text-2xl font-bold tracking-tight text-slate-900">
          Add a new service
        </h1>

        <p class="mt-1 text-sm text-slate-500">
          Create a service that customers can book with your business.
        </p>
      </header>

      <div class="p-6">
        <%= render "form", service: @service %>
      </div>
    </section>
  </div>
</main>